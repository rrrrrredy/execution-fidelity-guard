# Xiaohongshu image set v3

Visual direction: photographed engineering notebook and printed inspection
sheet, warm natural paper, graphite and red-pencil marks, restrained ink, no
neon gradients, glass panels, sci-fi HUD, humanoid robot, or decorative code.

## 01-cover-v3.png

Newly generated cover. Exact visible copy: product name; `contract BOUND`,
`balanced / install_local DENY`, `evidence CURRENT`,
`Windows/macOS/Linux CI PASS`; and the claim boundary
`工程校验通过，实际效果待真实任务验证`.

## 02-modes-v3.png

Newly generated behavior card. Exact visible semantics: `shadow：记录 / 提醒`,
`balanced：硬约束冲突 → 阻止`, `requires_user → 交还用户确认`; default shadow
does not block tool actions and records completion-evidence gaps; balanced is
the mode that asks or blocks.

## 03-evidence-chain-v3.png

Newly generated evidence card. It says `任务合同 → 当前证据 → 再说完成` and
separates `测试 / CI PASS`, `发布 / 外部核实`, and `跨平台 / CI PASS`. The
bottom line `工程证据 ≠ 实际效果` prevents CI evidence from being presented as
real-world efficacy.
